# The Elder Turtle Messaging Service - A tiny messaging service written in Python

## Setting up

Open `config.py` and insert your email, password, and preferred editor
(optional) into the fields (as Python strings).

## Running

To run the program, do `python main.py -h` for options. If there are no custom
parameters specified, it will run using the fields set up in `config.py`.

